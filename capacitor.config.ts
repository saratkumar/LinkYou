import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.lifelinker',
  appName: 'photo-gallery',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
